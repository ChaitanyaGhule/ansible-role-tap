---
# handlers file for nespresso_ansible_role_tap

- name: Restart Supervisor
  ansible.builtin.service:
    name: supervisord
    state: restarted
  become: true
  register: supervisor_restart
  failed_when: supervisor_restart.failed and 'not found' not in supervisor_restart.msg